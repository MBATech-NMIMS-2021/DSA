#include<iostream>
#include<vector>
#include<string>

using namespace std;

long sumArray(long array[], long arraySize){
    long sum = 0;
    for (long i = 0; i < arraySize; i++){
        sum += array[arraySize];
    }
    return sum;
}

int main(void){

    // Generating the array

    long arraySize;
    cout << "\nEnter the size of the array: ";
    cin >> arraySize;

    return 0;
}